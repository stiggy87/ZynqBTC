#include <stdio.h>
#include "sha256.h"

int main(int argc, char *argv[]) {
	//SHA256_CTX *ctx;
	//uchar data[64];
	//uchar hash[64];

	//fp = fopen("","w");

	//sha256_top(ctx, data, hash);
	//sha256_top(data, hash);

	//printf("Hash = %u",hash);
	printf("Test completed!");
	return 0;
}
